{% comment %}
  Section "Essai Virtuel" (VTO) - Lunetier
  Technologie : MediaPipe Face Mesh (468 landmarks) + Overlay Intelligent
  Features : Détection faciale temps réel, positionnement automatique, suivi visage
  Optimisé : Framerate 60 FPS desktop, 30 FPS mobile, résolution adaptative
{% endcomment %}

<style>
  :root {
    --vto-bg: #000000;
    --vto-accent: #ffffff;
    --vto-font: ui-sans-serif, system-ui, sans-serif;
  }

  .vto-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: var(--vto-bg);
    z-index: 9999;
    display: flex;
    flex-direction: column;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
    font-family: var(--vto-font);
    overflow: hidden;
  }

  .vto-modal.is-active {
    opacity: 1;
    pointer-events: auto;
  }

  /* Header */
  .vto-header {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    padding: 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    z-index: 20;
    background: linear-gradient(to bottom, rgba(0,0,0,0.8), transparent);
  }

  .vto-title {
    color: var(--vto-accent);
    font-size: 0.875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .vto-camera-icon {
    width: 20px;
    height: 20px;
    color: #ef4444;
    animation: pulse 2s infinite;
  }

  @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }

  .vto-close-btn {
    background: rgba(255,255,255,0.2);
    border: none;
    color: white;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(4px);
    transition: background 0.2s;
  }
  
  .vto-close-btn:hover { background: rgba(255,255,255,0.4); }

  /* Viewport */
  .vto-viewport {
    flex: 1;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #111;
    overflow: hidden;
  }

  #vto-video {
    position: absolute;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scaleX(-1);
  }

  #vto-canvas {
    position: absolute;
    width: 100%;
    height: 100%;
    transform: scaleX(-1);
    z-index: 10;
  }

  /* Loading */
  .vto-loading {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: white;
    z-index: 30;
  }

  .vto-spinner {
    width: 50px;
    height: 50px;
    border: 3px solid rgba(255,255,255,0.3);
    border-top-color: #ffffff;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
    margin: 0 auto 1rem;
  }

  @keyframes spin { to { transform: rotate(360deg); } }

  /* Error */
  .vto-error {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: white;
    display: none;
    z-index: 30;
    max-width: 20rem;
    padding: 2rem;
  }

  .vto-error h3 {
    margin: 0 0 1rem 0;
    font-size: 1.25rem;
  }

  .vto-error p {
    margin: 0 0 1.5rem 0;
    opacity: 0.8;
    line-height: 1.5;
  }

  .vto-btn {
    background: white;
    color: black;
    border: none;
    padding: 0.75rem 1.5rem;
    font-weight: 700;
    text-transform: uppercase;
    cursor: pointer;
    border-radius: 4px;
    transition: opacity 0.2s;
  }

  .vto-btn:hover { opacity: 0.9; }

  /* Controls */
  .vto-controls {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 2rem;
    background: linear-gradient(to top, rgba(0,0,0,0.9), transparent);
    z-index: 20;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .vto-instruction {
    color: rgba(255,255,255,0.9);
    font-size: 0.875rem;
    text-align: center;
    text-shadow: 0 2px 4px rgba(0,0,0,0.5);
  }

  .vto-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(0,0,0,0.5);
    padding: 0.5rem 1rem;
    border-radius: 99px;
    backdrop-filter: blur(10px);
    font-size: 0.75rem;
    color: rgba(255,255,255,0.8);
  }

  .vto-status-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #10b981;
  }

  .vto-status-dot.inactive { background: #ef4444; }

  .vto-sliders-group {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    width: 100%;
    max-width: 320px;
  }

  .vto-slider-wrapper {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: rgba(0,0,0,0.5);
    padding: 0.75rem 1.5rem;
    border-radius: 99px;
    backdrop-filter: blur(10px);
  }

  .vto-slider-label {
    color: white;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    min-width: 60px;
  }

  input[type=range] {
    -webkit-appearance: none;
    background: transparent;
    flex: 1;
  }

  input[type=range]::-webkit-slider-thumb {
    -webkit-appearance: none;
    height: 16px;
    width: 16px;
    border-radius: 50%;
    background: #ffffff;
    cursor: pointer;
    margin-top: -6px;
  }

  input[type=range]::-webkit-slider-runnable-track {
    width: 100%;
    height: 4px;
    background: rgba(255,255,255,0.3);
    border-radius: 2px;
  }

  .vto-fps {
    position: absolute;
    top: 5rem;
    right: 1.5rem;
    background: rgba(0,0,0,0.7);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 4px;
    font-size: 0.75rem;
    font-family: monospace;
    backdrop-filter: blur(10px);
    z-index: 20;
  }

  .vto-device-info {
    position: absolute;
    top: 8rem;
    right: 1.5rem;
    background: rgba(0,0,0,0.7);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 4px;
    font-size: 0.65rem;
    font-family: monospace;
    backdrop-filter: blur(10px);
    z-index: 20;
    line-height: 1.4;
  }

</style>

<div id="vto-modal" class="vto-modal" aria-hidden="true">
  
  <!-- Header -->
  <div class="vto-header">
    <div class="vto-title">
      <svg class="vto-camera-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"></path>
        <circle cx="12" cy="13" r="4"></circle>
      </svg>
      Essai Virtuel
    </div>
    <button class="vto-close-btn" id="vto-close" aria-label="Fermer">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="18" y1="6" x2="6" y2="18"></line>
        <line x1="6" y1="6" x2="18" y2="18"></line>
      </svg>
    </button>
  </div>

  <!-- FPS Counter -->
  <div class="vto-fps" id="vto-fps">FPS: --</div>
  
  <!-- Device Info -->
  <div class="vto-device-info" id="vto-device-info"></div>

  <!-- Viewport -->
  <div class="vto-viewport">
    <video id="vto-video" autoplay playsinline muted></video>
    <canvas id="vto-canvas"></canvas>

    <!-- Loading -->
    <div id="vto-loading" class="vto-loading">
      <div class="vto-spinner"></div>
      <p>Initialisation de la caméra...</p>
    </div>

    <!-- Error -->
    <div id="vto-error-msg" class="vto-error">
      <h3>Caméra inaccessible</h3>
      <p>Veuillez autoriser l'accès à la caméra pour utiliser l'essai virtuel.</p>
      <button class="vto-btn" onclick="window.vtoStartCamera()">Réessayer</button>
    </div>
  </div>

  <!-- Controls -->
  <div class="vto-controls">
    <p class="vto-instruction">Ajustez la position et la taille des lunettes</p>
    
    <!-- Status -->
    <div class="vto-status">
      <div class="vto-status-dot" id="vto-status-dot"></div>
      <span id="vto-status-text">Recherche de visage...</span>
    </div>

    <!-- Sliders Group -->
    <div class="vto-sliders-group">
      <!-- Scale Control -->
      <div class="vto-slider-wrapper">
        <span class="vto-slider-label">Taille</span>
        <input type="range" id="vto-scale" min="50" max="150" value="100" step="5">
        <span class="vto-slider-label" id="vto-scale-value" style="min-width:40px; text-align:right;">100%</span>
      </div>
      
      <!-- Y Position Control -->
      <div class="vto-slider-wrapper">
        <span class="vto-slider-label">Position Y</span>
        <input type="range" id="vto-yoffset" min="-20" max="20" value="0" step="1">
        <span class="vto-slider-label" id="vto-yoffset-value" style="min-width:40px; text-align:right;">0</span>
      </div>
    </div>
  </div>

</div>

<!-- MediaPipe Face Mesh CDN -->
<script src="https://cdn.jsdelivr.net/npm/@mediapipe/face_mesh/face_mesh.js" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/@mediapipe/camera_utils/camera_utils.js" crossorigin="anonymous"></script>

<script>
(function() {
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initVTO);
  } else {
    initVTO();
  }

  function initVTO() {
    const modal = document.getElementById('vto-modal');
    const closeBtn = document.getElementById('vto-close');
    const video = document.getElementById('vto-video');
    const canvas = document.getElementById('vto-canvas');
    const ctx = canvas.getContext('2d', { alpha: true, desynchronized: true, willReadFrequently: false });
    const scaleInput = document.getElementById('vto-scale');
    const scaleValue = document.getElementById('vto-scale-value');
    const yOffsetInput = document.getElementById('vto-yoffset');
    const yOffsetValue = document.getElementById('vto-yoffset-value');
    const statusDot = document.getElementById('vto-status-dot');
    const statusText = document.getElementById('vto-status-text');
    const loadingEl = document.getElementById('vto-loading');
    const errorMsg = document.getElementById('vto-error-msg');
    const fpsEl = document.getElementById('vto-fps');
    const deviceInfoEl = document.getElementById('vto-device-info');
    
    if (!modal || !video || !canvas) return;

    // Détection mobile
    const isMobile = /Android|iPhone|iPad|iPod|Opera Mini|IEMobile|WPDesktop/i.test(navigator.userAgent) || window.innerWidth < 768;
    
    // Config adaptative selon device
    const config = {
      width: isMobile ? 640 : 1280,
      height: isMobile ? 480 : 720,
      fps: isMobile ? 30 : 60,
      minDetectionConfidence: isMobile ? 0.6 : 0.7,
      minTrackingConfidence: isMobile ? 0.6 : 0.7,
      refineLandmarks: !isMobile,
      smoothingFactor: isMobile ? 0.6 : 0.5
    };

    let faceMesh = null;
    let camera = null;
    let glassesImg = null;
    let glassesAspectRatio = 1;
    let scaleMultiplier = 1.0;
    let yOffsetAdjust = 0; // Ajustement Y manuel
    let faceDetected = false;
    let isRunning = false;
    
    // Smoothing
    let smoothedLandmarks = null;

    // FPS counter
    let lastFrameTime = Date.now();
    let frameCount = 0;
    let fps = 0;

    // Device info
    if (deviceInfoEl) {
      deviceInfoEl.innerHTML = `Device: ${isMobile ? 'Mobile' : 'Desktop'}<br>Res: ${config.width}x${config.height}<br>Target: ${config.fps} FPS<br>Refine: ${config.refineLandmarks ? 'ON' : 'OFF'}`;
    }

    // Précharger l'image des lunettes ET calculer son ratio
    {% if product.featured_media %}
    glassesImg = new Image();
    glassesImg.crossOrigin = 'anonymous';
    glassesImg.src = '{{ product.featured_media | image_url: width: 800 }}';
    glassesImg.onload = () => {
      glassesAspectRatio = glassesImg.naturalWidth / glassesImg.naturalHeight;
      console.log(`✅ Glasses loaded: ${glassesImg.naturalWidth}x${glassesImg.naturalHeight} (ratio: ${glassesAspectRatio.toFixed(2)})`);
    };
    {% endif %}

    // --- OUVERTURE / FERMETURE ---
    
    window.openVirtualTryOn = async function() {
      console.log(`✅ Opening VTO (${isMobile ? 'Mobile' : 'Desktop'} mode)...`);
      modal.classList.add('is-active');
      document.body.style.overflow = 'hidden';
      isRunning = true;
      await startVTO();
    };

    window.vtoStartCamera = async function() {
      isRunning = true;
      await startVTO();
    };

    if (closeBtn) {
      closeBtn.addEventListener('click', () => {
        stopVTO();
      });
    }

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && modal.classList.contains('is-active')) {
        stopVTO();
      }
    });

    // --- CONTROLS ---
    
    if (scaleInput) {
      scaleInput.addEventListener('input', (e) => {
        const value = parseFloat(e.target.value);
        scaleMultiplier = value / 100;
        if (scaleValue) scaleValue.textContent = value + '%';
      });
    }

    if (yOffsetInput) {
      yOffsetInput.addEventListener('input', (e) => {
        yOffsetAdjust = parseFloat(e.target.value);
        if (yOffsetValue) yOffsetValue.textContent = yOffsetAdjust;
      });
    }

    // --- MEDIAPIPE FACE MESH ---
    
    async function startVTO() {
      try {
        loadingEl.style.display = 'block';
        errorMsg.style.display = 'none';
        smoothedLandmarks = null;
        
        if (!faceMesh) {
          faceMesh = new FaceMesh({
            locateFile: (file) => {
              return `https://cdn.jsdelivr.net/npm/@mediapipe/face_mesh/${file}`;
            }
          });

          faceMesh.setOptions({
            maxNumFaces: 1,
            refineLandmarks: config.refineLandmarks,
            minDetectionConfidence: config.minDetectionConfidence,
            minTrackingConfidence: config.minTrackingConfidence
          });

          faceMesh.onResults(onFaceMeshResults);
        }

        if (!camera) {
          camera = new Camera(video, {
            onFrame: async () => {
              if (isRunning) {
                await faceMesh.send({ image: video });
              }
            },
            width: config.width,
            height: config.height,
            fps: config.fps
          });
        }

        await camera.start();
        loadingEl.style.display = 'none';
        console.log(`✅ VTO started: ${config.width}x${config.height} @ ${config.fps} FPS`);

      } catch (err) {
        console.error('❌ VTO error:', err);
        loadingEl.style.display = 'none';
        errorMsg.style.display = 'block';
      }
    }

    function stopVTO() {
      modal.classList.remove('is-active');
      document.body.style.overflow = '';
      isRunning = false;
      
      if (camera) {
        camera.stop();
      }
      
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      faceDetected = false;
      smoothedLandmarks = null;
      updateStatus(false);
      console.log('🛑 VTO stopped');
    }

    // --- SMOOTHING FUNCTION ---
    
    function smoothLandmarks(newLandmarks) {
      if (!smoothedLandmarks) {
        smoothedLandmarks = newLandmarks;
        return newLandmarks;
      }

      const smoothed = newLandmarks.map((landmark, i) => ({
        x: smoothedLandmarks[i].x + (landmark.x - smoothedLandmarks[i].x) * config.smoothingFactor,
        y: smoothedLandmarks[i].y + (landmark.y - smoothedLandmarks[i].y) * config.smoothingFactor,
        z: smoothedLandmarks[i].z + (landmark.z - smoothedLandmarks[i].z) * config.smoothingFactor
      }));

      smoothedLandmarks = smoothed;
      return smoothed;
    }

    // --- FACE MESH RESULTS ---
    
    function onFaceMeshResults(results) {
      // FPS counter
      frameCount++;
      const now = Date.now();
      if (now - lastFrameTime >= 1000) {
        fps = frameCount;
        frameCount = 0;
        lastFrameTime = now;
        if (fpsEl) fpsEl.textContent = `FPS: ${fps}`;
      }

      // Resize canvas to match video
      if (canvas.width !== results.image.width || canvas.height !== results.image.height) {
        canvas.width = results.image.width;
        canvas.height = results.image.height;
      }

      // Clear canvas
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      if (results.multiFaceLandmarks && results.multiFaceLandmarks.length > 0) {
        const rawLandmarks = results.multiFaceLandmarks[0];
        const landmarks = smoothLandmarks(rawLandmarks);
        
        if (!faceDetected) {
          faceDetected = true;
          updateStatus(true);
        }

        drawGlasses(landmarks, canvas.width, canvas.height);
      } else {
        if (faceDetected) {
          faceDetected = false;
          updateStatus(false);
        }
      }
    }

    // --- DRAW GLASSES (IMPROVED POSITIONING) ---
    
    function drawGlasses(landmarks, width, height) {
      if (!glassesImg || !glassesImg.complete) return;

      // ✅ Landmarks optimisés pour lunettes
      // 33 = coin extérieur oeil gauche
      // 263 = coin extérieur oeil droit  
      // 159 = coin intérieur oeil gauche (plus précis pour centre)
      // 386 = coin intérieur oeil droit
      // 168 = milieu entre les yeux (pont du nez)
      
      const leftEyeOuter = landmarks[33];
      const rightEyeOuter = landmarks[263];
      const leftEyeInner = landmarks[159];
      const rightEyeInner = landmarks[386];
      const noseBridge = landmarks[168]; // Point clé : pont du nez

      // Distance entre coins extérieurs des yeux
      const eyeDistance = Math.sqrt(
        Math.pow((rightEyeOuter.x - leftEyeOuter.x) * width, 2) +
        Math.pow((rightEyeOuter.y - leftEyeOuter.y) * height, 2)
      );

      // Centre X : milieu entre coins extérieurs
      const centerX = ((leftEyeOuter.x + rightEyeOuter.x) / 2) * width;
      
      // ✅ Centre Y amélioré : utilise le pont du nez comme référence
      // Les lunettes doivent reposer sur le pont du nez, pas flotter au-dessus
      const eyeCenterY = ((leftEyeOuter.y + rightEyeOuter.y) / 2) * height;
      const noseBridgeY = noseBridge.y * height;
      
      // Position Y = moyenne entre centre des yeux et pont du nez
      // Cela positionne les lunettes exactement où elles doivent être
      const centerY = (eyeCenterY + noseBridgeY) / 2;

      // Angle de rotation
      const angle = Math.atan2(
        (rightEyeOuter.y - leftEyeOuter.y) * height,
        (rightEyeOuter.x - leftEyeOuter.x) * width
      );

      // Dimensions avec ratio préservé
      const glassesWidth = eyeDistance * 2.5 * scaleMultiplier;
      const glassesHeight = glassesWidth / glassesAspectRatio;

      // ✅ Y offset ajustable : permet un fine-tuning manuel
      // Valeur de base réduite car on utilise déjà le pont du nez
      const baseYOffset = eyeDistance * 0.05; // Réduit de 0.15 à 0.05
      const manualYOffset = yOffsetAdjust * (eyeDistance * 0.01); // 1% par unité
      const finalYOffset = baseYOffset + manualYOffset;

      // Draw
      ctx.save();
      ctx.imageSmoothingEnabled = true;
      ctx.imageSmoothingQuality = isMobile ? 'medium' : 'high';
      ctx.translate(centerX, centerY - finalYOffset);
      ctx.rotate(angle);
      ctx.drawImage(
        glassesImg,
        -glassesWidth / 2,
        -glassesHeight / 2,
        glassesWidth,
        glassesHeight
      );
      ctx.restore();
    }

    // --- UPDATE STATUS ---
    
    function updateStatus(detected) {
      if (detected) {
        statusDot.classList.remove('inactive');
        statusText.textContent = 'Visage détecté';
      } else {
        statusDot.classList.add('inactive');
        statusText.textContent = 'Recherche de visage...';
      }
    }

    console.log(`✅ VTO initialized (${isMobile ? 'Mobile' : 'Desktop'})`);
  }
})();
</script>

{% schema %}
{
  "name": "Essai Virtuel MediaPipe",
  "settings": [],
  "presets": [
    {
      "name": "Essai Virtuel"
    }
  ]
}
{% endschema %}